/** Tailwind v4 via PostCSS. The theme is OUR tokens (app/tokens.css) mapped in
 * globals.css `@theme inline` — never the default zinc look. */
const config = {
  plugins: {
    "@tailwindcss/postcss": {}
  }
};

export default config;
